<!DOCTYPE html>
<html>
<head>
  <title>Story Collab</title>
</head>
<body>
<link rel="stylesheet" href="/style.css">

<div class="container">
    <header class="app-header">
  <h1>Story Collab</h1>
  <p class="tagline">Write stories together, one chapter at a time</p>
</header>


  <h1>Collaborative Stories</h1>

  <a href="/new">Create New Story</a>

  <hr />

  <% if (stories.length === 0) { %>
    <p>No stories yet. Create one!</p>
  <% } %>

  <ul>
  <% stories.forEach(story => { %>
    <li>
      <a href="/stories/<%= story._id %>">
        <%= story.title %>
      </a>

      <form
  action="/stories/<%= story._id %>/delete"
  method="POST"
  style="display:inline;"
  onsubmit="return confirm('Are you sure you want to delete this story?');"
>
  <button class="delete-btn" title="Delete story">🗑</button>

</form>

    </li>
  <% }) %>
</ul>


</div>


</body>
</html>
